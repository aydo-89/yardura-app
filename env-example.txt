#############################################
# Yardura Environment Variables (Example)
# Copy to .env.local and fill in values
#############################################

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database (Prisma)
# For local dev you can use SQLite (see prisma/schema.prisma) or Postgres
# DATABASE_URL=postgresql://user:pass@localhost:5432/yardura
DATABASE_URL=file:./prisma/dev.db

# Auth (NextAuth)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change_me_min_32_chars________________________________
ADMIN_EMAILS=admin@example.com

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Email (choose one: Resend or SMTP)
EMAIL_FROM=noreply@example.com
RESEND_API_KEY=
EMAIL_SERVER_HOST=
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=
EMAIL_SERVER_PASSWORD=

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Supabase (optional; used for storage/DB helpers)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
STORAGE_BUCKET=stool-samples

# Redis (optional; queues/rate limits)
REDIS_URL=

# Edge device provisioning (optional; for Pi ingest)
EDGE_DEVICE_ISSUER=yardura-device
JWT_SIGNING_KEY=change_me_signing_key_________________________________

# Security / Analytics (optional)
SENTRY_DSN=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# reCAPTCHA (optional)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Google Maps (optional)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Feature flags
ENABLE_INSIGHTS=false
ENABLE_DEVICE_INTEGRATION=false
ENABLE_MULTI_TENANT=false
